# Assets

`logo.svg` (black, for light backgrounds) and `logo-dark.svg` (light fill, for
dark backgrounds / favicons) are the project logo.

Screenshots (one per view, captured from the dev container) are referenced by
the README, landing page, and docs: `zones.png`, `services.png`, `ports.png`,
`forward.png`, `rich-rules.png`, `interfaces.png`, `sources.png`, `ipsets.png`,
`direct.png`, `logs.png`.
